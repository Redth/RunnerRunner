<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.runnerrunner.agent</string>

    <key>ProgramArguments</key>
    <array>
        <string>/opt/runnerrunner/RunnerRunner.Agent</string>
    </array>

    <key>WorkingDirectory</key>
    <string>/opt/runnerrunner</string>

    <!-- Run as the deploy user so tart/docker see the user's images -->
    <key>UserName</key>
    <string>redth</string>

    <!-- Start on boot -->
    <key>RunAtLoad</key>
    <true/>

    <!-- Restart if it crashes -->
    <key>KeepAlive</key>
    <true/>

    <!-- Wait 5 seconds before restarting after crash -->
    <key>ThrottleInterval</key>
    <integer>5</integer>

    <!-- Environment variables from file -->
    <key>EnvironmentVariables</key>
    <dict>
        <key>DOTNET_ENVIRONMENT</key>
        <string>Production</string>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
        <key>HOME</key>
        <string>/Users/redth</string>
    </dict>

    <!-- Logging -->
    <key>StandardOutPath</key>
    <string>/var/log/runnerrunner-agent.log</string>
    <key>StandardErrorPath</key>
    <string>/var/log/runnerrunner-agent.error.log</string>
</dict>
</plist>
